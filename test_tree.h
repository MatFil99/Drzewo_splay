#pragma once

#include "tree_splay.h"
#include "node_tree.h"

template<typename KeyType, typename ValueType>
void insert_test(TreeSplay<KeyType, ValueType>& t){
    
}

template<typename KeyType, typename ValueType>
void clear_test(TreeSplay<KeyType, ValueType>& tree){
    
}

template<typename KeyType, typename ValueType>
void erase_test(TreeSplay<KeyType, ValueType>& tree){

}